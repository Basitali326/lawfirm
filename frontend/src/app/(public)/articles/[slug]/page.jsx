"use client";
import { useParams } from "next/navigation";
import { useEffect, useState } from "react";
import { API_BASE_URL } from "@/lib/config";
export default function ArticleDetailPage(){const {slug}=useParams();const [item,setItem]=useState(null);useEffect(()=>{const firm=process.env.NEXT_PUBLIC_STOREFRONT_FIRM_SLUG||"";fetch(`${API_BASE_URL}/api/v1/website/articles/${slug}/${firm?`?firm_slug=${firm}`:""}`).then(r=>r.ok?r.json():null).then(p=>setItem(p?.data||null));},[slug]);if(!item)return <main className="mx-auto max-w-4xl px-5 py-20 text-slate-500">Loading article…</main>;return <main><header className="bg-[#111b2d] px-5 py-20 text-white"><div className="mx-auto max-w-4xl"><p className="text-xs font-bold uppercase tracking-[0.25em] text-[#dfc18b]">{item.category_name}</p><h1 className="mt-5 font-serif text-5xl leading-tight">{item.title}</h1><p className="mt-5 text-slate-300">By {item.author_name}</p></div></header><article className="mx-auto max-w-4xl px-5 py-16">{item.featured_image_url?<img src={item.featured_image_url} alt={item.title} className="mb-10 aspect-video w-full object-cover" />:null}<p className="whitespace-pre-wrap text-lg leading-9 text-slate-700">{item.content}</p></article></main>}
